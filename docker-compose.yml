version: "3.4"

services:
  web:
    image: nginx
    ports:
        - "80:80"
    volumes:
      - ./Public:/code
      - ./nginx/default.conf:/etc/nginx/nginx.conf:ro
    networks:
      - nginx-net
networks:
  nginx-net: